# ============================================================
# CADD Centre Unified — Environment Variables
# .env.local is already configured with Project 1's database.
# ============================================================

# Supabase — same database as Project 1 (already set in .env.local)


# NEXT_PUBLIC_SUPABASE_URL=supabase url
# NEXT_PUBLIC_SUPABASE_ANON_KEY=supabase anon key

# DATABASE SETUP:
# Run DB_IMS_ADDON.sql in Supabase SQL Editor to add IMS tables.
# This is safe — it never touches existing ASMS tables or data.
